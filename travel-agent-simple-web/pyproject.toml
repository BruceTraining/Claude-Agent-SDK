[project]
name = "flight-agent-naive-web"
version = "0.1.0"
description = "A flight booking agent served via Flask — demonstrating why query()"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "claude-agent-sdk>=0.2.0",
    "python-dotenv>=1.0.0",
    "flask>=3.0.0",
]
